package main

//account resolver will have two queries one to get all the orders and another to get order by id
// Orders

// OrderByID

type accountResolver struct {
	server *Server
}

// func (r *accountResolver) Orders(ctx context.Context, obj *Account) ([]*Order, error) {
// }
